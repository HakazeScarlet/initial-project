package learn_oop.animal;

public class Main {

    public static void main(String[] args) {

        Animal animal = new Animal(); // create new object
        // in memory -> 0x567621: [value] -> 0x567621: [Animal]

        Cat cat = new Cat(); // create Cat object
        cat.say("Meow"); // call method (from Animal class)
    }
}

// video 3
    // new java keyword (in memory)
    // constructor for class, constructor by default
    // class & object

    // inheritance (extends)
    // interface in java (implements)? interface VS class
    // access modifiers (private, public, protected, package-private)

// video 4
    // methods overloading - перегрузка методов


